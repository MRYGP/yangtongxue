本文档和项目无关，是一个观察者。但可以时常更新

1 ChatGPT 负责做两件事
 把需求写成可验收的规格
 把改动写成 Cursor 可执行的修改指令

2 GPTS 负责做两件事
 读仓库拿证据
 按模板产出周计划 周复盘 日记录与入库稿

3 Cursor 负责做两件事
 把改动落到文件
 做快速自检并提交 commit

4 GitHub 负责做一件事
 做唯一可信来源
 所有口头结论都必须落到文件才能算完成

常见坑清单

- 指令里出现括号类符号，模型输出自检会回环
- Attempt Log 行数上限太紧，STOP 一加就超
- Source 出现多次或出现在非验收块，触发自检重写
- API 可读但 base64 解码失败，要直接用同一 JSON 的 download_url
- 同一 URL 失败后换理由重试，会浪费打开次数
- INDEX 写成 URL 链接而不是相对路径，会违反 URL 输出禁令
- 日志里写了可识别信息，后期很难清洗